package com.pawfinder.notification.service;

import org.springframework.stereotype.Service;

/**
 * Service responsible for sending push notifications via Firebase Cloud Messaging (FCM).
 *
 * TODO: Integrate Firebase Admin SDK for push notification delivery
 *       - Register device tokens per user
 *       - Send targeted push notifications for nearby lost-pet alerts
 *       - Handle notification delivery receipts and failures
 */
@Service
public class PushNotificationService {

    // Stub – implementation pending Firebase Admin SDK integration
}
