package com.pawfinder.notification.service;

import org.springframework.stereotype.Service;

/**
 * Service responsible for sending email notifications via SendGrid.
 *
 * TODO: Integrate SendGrid SDK for transactional email delivery
 *       - Send lost-pet alert emails to subscribed users
 *       - Send account verification and password reset emails
 *       - Send reward claim status updates
 *       - Use SendGrid dynamic templates for branded emails
 */
@Service
public class EmailService {

    // Stub – implementation pending SendGrid integration
}
